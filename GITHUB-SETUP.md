# GitHub repository setup

## Recommended repository name

```text
archer-c6-v3-openwrt-suite
```

## Recommended description

```text
A custom OpenWrt first-boot suite for TP-Link Archer C6 V3 with a LuCI dashboard, guided WAN setup, Smart Connect-style Wi-Fi, guest isolation, per-device controls, CAKE SQM, Adblock, Watchcat, and monthly traffic accounting.
```

## Suggested topics

```text
openwrt
tp-link
archer-c6
archer-c6-v3
luci
router
networking
sqm
cake
nftables
adblock
usteer
bandwidth-monitor
```

## Suggested first release

- Tag: `v1.0.0`
- Title: `Archer C6 V3 OpenWrt Suite v1.0.0`
- Attach: `firstboot.sh`, `packages.txt`, and the repository ZIP.

## Repository settings

- Enable Issues.
- Enable Discussions only when you want community support threads.
- Require pull requests before merging to the default branch.
- Enable secret scanning and push protection when available.
- Add `main` branch protection after the initial commit.

## Screenshot workflow

Replace the placeholder SVG files inside `screenshots/` with real screenshots using the same filenames, or edit the image paths in `README.md`.
